import * as sizeAction from '../size/action';
import * as sizeTypes from '../size/type';
import sizeReducer from '../size/reducer';
export {
    sizeAction,
    sizeTypes
  };
  export default sizeReducer;
